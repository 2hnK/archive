---
import BaseLayout from "../layouts/BaseLayout.astro";
import ArticleCard from "../components/ArticleCard.astro";
import { getCollection } from "astro:content";

const allArticles = await getCollection("articles");
const sortedArticles = allArticles.sort((a, b) => b.data.date.valueOf() - a.data.date.valueOf());
const featured = sortedArticles.find((a) => a.data.isFeatured) || sortedArticles[0];
const latestArticles = sortedArticles.slice(0, 6);
---

<BaseLayout title="DevArchive" description="모던하고 미니멀한 개발자 커리어 로그">
  <main class="pt-24 pb-20">
    <!-- Greeting -->
    <section class="max-w-[1200px] mx-auto px-6 pt-12 pb-20 fade-in-up">
      <div class="max-w-3xl">
        <span
          class="text-[11px] tracking-[0.25em] font-bold text-apple-textMuted dark:text-apple-darkTextMuted uppercase mb-2 block"
          >Developer Blog</span
        >
        <h1
          class="text-[4rem] md:text-[5.5rem] font-bold tracking-tighter text-apple-text dark:text-apple-darkText leading-[1.05] mb-2 break-keep"
        >
          Hello, World!
        </h1>
        <p
          class="text-[19px] text-apple-textMuted dark:text-apple-darkTextMuted leading-[1.6] max-w-[650px] break-keep font-medium flex items-center gap-2"
        >
          커피를 코드로 변환하는 중...
          <span
            class="flex items-center gap-1 ml-1 bg-black/[0.03] dark:bg-white/[0.05] px-2 py-0.5 rounded-full ring-1 ring-black/5 dark:ring-white/10"
          >
            <span
              class="material-symbols-outlined text-[18px] text-amber-700/80 dark:text-amber-500/80"
              >local_cafe</span
            >
            <span class="material-symbols-outlined text-[14px] text-gray-600 dark:text-gray-400"
              >trending_flat</span
            >
            <span class="material-symbols-outlined text-[18px] text-blue-600 dark:text-blue-400"
              >terminal</span
            >
          </span>
        </p>
      </div>
    </section>

    <!-- Featured Post -->
    {
      featured && (
        <section class="max-w-[1200px] mx-auto px-6 mb-32 reveal-up stagger-1">
          <span class="text-[11px] tracking-[0.25em] font-bold text-apple-textMuted dark:text-apple-darkTextMuted uppercase mb-4 block">
            Featured Journal
          </span>
          <a
            href={`/articles/${featured.id}`}
            class="group block relative overflow-hidden rounded-[2rem] bg-black/5 dark:bg-white/5 aspect-[16/10] md:aspect-[21/9] border border-apple-border/50 dark:border-apple-darkBorder/50 shadow-[0_8px_32px_rgba(0,0,0,0.06)]"
          >
            {featured.data.imageUrl ? (
              <img
                alt={featured.data.title}
                class="absolute inset-0 w-full h-full object-cover group-hover:scale-[1.03] transition-transform duration-1000 ease-[cubic-bezier(0.16,1,0.3,1)]"
                src={featured.data.imageUrl}
                loading="lazy"
              />
            ) : (
              <div class="absolute inset-0 w-full h-full bg-apple-surface dark:bg-apple-darkSurface flex items-center justify-center">
                <span class="material-symbols-outlined text-[60px] text-apple-border dark:text-apple-darkBorder">
                  history
                </span>
              </div>
            )}
            <div class="absolute inset-0 bg-gradient-to-t from-black/80 via-black/20 to-transparent" />
            <div class="absolute bottom-0 left-0 right-0 p-8 md:p-12 z-10 transition-transform duration-700 ease-[cubic-bezier(0.16,1,0.3,1)] group-hover:translate-y-[-4px]">
              <div class="mb-4">
                <span class="bg-white/20 backdrop-blur-md text-white px-3 py-1.5 rounded-full text-[10px] font-bold uppercase tracking-[0.2em] shadow-sm border border-white/20">
                  {featured.data.category}
                </span>
              </div>
              <h2 class="text-[2.2rem] md:text-[3.5rem] font-bold tracking-tighter text-white mb-4 leading-[1.1] max-w-3xl break-keep group-hover:text-orange -300 transition-colors">
                {featured.data.title}
              </h2>
              <p class="text-white/80 text-[15px] md:text-[17px] font-medium max-w-2xl break-keep line-clamp-2">
                {featured.data.description}
              </p>
            </div>
          </a>
        </section>
      )
    }

    <!-- Latest Articles Grid -->
    <section class="max-w-[1200px] mx-auto px-6 reveal-up stagger-2">
      <div
        class="flex justify-between items-end mb-12 border-b border-apple-border dark:border-apple-darkBorder pb-6"
      >
        <div>
          <span
            class="text-[11px] uppercase tracking-[0.25em] font-bold text-apple-textMuted dark:text-apple-darkTextMuted block mb-2"
            >Latest Insights</span
          >
          <h2
            class="text-[2rem] font-bold tracking-tighter text-apple-text dark:text-apple-darkText"
          >
            최근 포스트
          </h2>
        </div>
        <a href="/articles" class="btn-premium group">
          아티클 전체 보기
          <span
            class="material-symbols-outlined text-[16px] group-hover:translate-x-1 transition-transform"
            >arrow_forward</span
          >
        </a>
      </div>

      <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-x-8 gap-y-16">
        {
          latestArticles.map((article) => (
            <ArticleCard
              title={article.data.title}
              description={article.data.description}
              category={article.data.category}
              date={article.data.date.toLocaleDateString("ko-KR")}
              href={`/articles/${article.id}`}
              imageUrl={article.data.imageUrl || ""}
            />
          ))
        }
      </div>
    </section>
  </main>

  <script>
    document.addEventListener("astro:page-load", () => {
      const revealObserver = new IntersectionObserver(
        (entries, observer) => {
          entries.forEach((entry) => {
            if (entry.isIntersecting) {
              entry.target.classList.add("active", "translate-y-0", "opacity-100");
              entry.target.classList.remove("translate-y-8", "opacity-0");
              observer.unobserve(entry.target);
            }
          });
        },
        { root: null, rootMargin: "0px 0px 100px 0px", threshold: 0 },
      );

      const els = document.querySelectorAll(".reveal-up, .fade-in-up");
      els.forEach((el) => {
        // Only hide if not already marked active (prevent double-hide on route change)
        if (!el.classList.contains("active")) {
          el.classList.add(
            "transition-all",
            "duration-1000",
            "ease-[cubic-bezier(0.16,1,0.3,1)]",
            "opacity-0",
            "translate-y-8",
          );
        }
        revealObserver.observe(el);
      });
    });
  </script>
</BaseLayout>
